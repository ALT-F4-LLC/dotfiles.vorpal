# Changelog: model-distribution/team-lead.md

## 2026-06-30

### Summary
Conformance audit of 431 spawns (7d) found zero routing defects — the Tiers prose is being followed. An operator-directed quality track then institutionalized a routing-quality dimension (Phase-1 class 6 "quality-mismatch") into the evolve-model-distribution skill and applied 3 team-lead.md Tiers refinements. Phase-3 coherence: all PASS, no fix.

### Routing Changes
- QUALITY: SPLIT sdet test-authoring — routine (add-a-case / extend-suite) = sonnet / new test-architecture / harness = opus; grounded in sdet.md `effort: xhigh` + @sdet ownership of test-architecture (defect-class if under-tiered: silent-passing implementation-coupled tests). Prophylactic (0 measured architecture spawns) — admitted on capability-match reasoning, not a measured harm count.
- QUALITY: DISAMBIGUATE the impl Medium|Large seam — opus bullet now defines "Large = touches >=3 modules OR a new seam OR >1-day horizon"; grounded in cross-session non-determinism (same DKT-id measured at both tiers). No tier moved.
- Trial: docs-researcher opus→sonnet — Hypothesis: pure doc retrieval/summarization needs no opus depth; sonnet equal quality at lower cost. Applied as a revert-on-rollback TRIAL line in team-lead.md Tiers. Measure next cycle for quality regression (operator corrections, -r2, weak-summary-traceable design errors); adopt if none, rollback if quality drops. Scope: docs-researcher only — investigator/innovation-scanner stay opus.

### Evidence
Quality edits grounded in role-definition reasoning (sdet.md effort:xhigh + test-architecture ownership; impl size-gate) via the class-6 capability-adding lane, NOT session measurement. Conformance audit: 431 spawns / 59 subagents dirs (7d, cutoff 2026-06-24T03:57:35Z); hard-floor roles all opus; no named fallback-drift; no policy-stale. Mimir available (7d cost: opus $2,476 / sonnet-4-6 $87 / sonnet-5 $73 / haiku $1.73) — aggregate, non-role-attributable, so no cost downgrade justified.

### Rejected
- Conformance model-tier edits: None (routing conformant).
- Cost / over-provisioning downgrades: rejected — Mimir cost aggregate / non-role-attributable, no per-role signal (Improvement-Only Mandate).
