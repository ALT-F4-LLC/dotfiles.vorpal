- Posting a Docket comment whose body discusses this repo's git-guard hook (probe strings, verb examples) → PreToolUse deny (exit 2) on the comment command itself, in auto mode with no retry path → root cause: the deployed guard-no-commit-hook.sh matches git-write-shaped prose in the command text, and a `docket ... -m "<body>"` carrying `$(`/backtick content is emitted bare to the matcher → resolution: write the body to a scratchpad file and post via `docket issue comment add <id> -m "$(cat <file>)"` — the command text then contains only the path, never the payload (documented escape hatch; recurs for every advisor/reviewer spawn writing hook-related evidence comments on DKT-175/182/189-family issues).
