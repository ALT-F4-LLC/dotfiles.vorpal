# M4 disposition — RUN-3 (07 §4 step 5)

Drafted by the reviewer 2026-08-06; ratified by the operator 2026-08-06.
Evidence: m4-preregistration.md (the frame) and m4-findings.md (the record).

## Verdict

**Halt-with-cause, honestly taken — and adopt-and-iterate for the system.**
The run halted per the pre-registration's own rule when two engine gaps
composed into a mechanical wedge (E-12). The halt is not the system failing
its test; it is the shadow run producing exactly what it was pre-registered
to produce: a verdict on readiness with evidence. The architecture is
adopted; the engine iterates before any rerun.

## The dimensions, judged

- **AC-1 (judgment by LLMs, carried in briefs):** MET, live-proven. Packet
  composition delivered contracts+fragments into real briefs (P-9); judges
  probe-proved findings a human gate had passed (P-10); syntheses verified
  severity multisets and corrected a reviewer's false premise by source-read
  (P-7 correction). The corpus steers real work.
- **AC-2 (deterministic work by Docket):** MET in every observed mechanism —
  atomic claims under contention, CAS dispatch, byte-verified manifests,
  engine-run gates, budget floor — with the events-query confirmation
  CONFIRMED (docket events list --run RUN-3 --json, 117 events,
  2026-08-06): every kind is engine lifecycle vocabulary — run/dispatch/
  step/gate events plus operator-attributed resolves. No model appears in
  the log; agents only claimed and recorded; all routing engine-computed.
  The log also self-documents the run's pathologies: three
  gate-unmatched:scope events (P-1) and the accepted-missing-usage lists
  growing 1→12 across closes (E-4's re-acceptance, visible in the ledger
  itself).
- **Zero-touch (T9):** NOT MET this run, honestly recorded. Interventions far
  exceeded the pre-registered five classes (register §4). Nearly all were
  first-run infrastructure findings, not steady-state behavior — but the
  claim stays unmet until a run meets it.
- **Bands:** cost HELD (12.9/30 declared; ~802k output tokens measured —
  well inside ~$50). Wall-clock exceeded the afternoon framing; attention-
  hours dominated by finding-handling, which is what shadow runs are for.
- **Correctness floor:** UNBROKEN. Zero state corruption, zero double-work,
  zero fabricated records across 53 spawns, 21 conflict losses, 4 pre-spawn
  failures, and an abandon.

## The cause, and its disposition (the rerun condition)

E-12 = E-4 (acceptance cannot clear the computed D2 probe) × next-only
action driving (next.go:137 vs :205), fed by E-3 (no usage back-fill verb).
**No rerun until the cluster lands engine-side**, via the arc's own cadence
(patch session or RUN-4-on-a-fixed-binary bootstrap): drive actions
independent of the discrepancy refusal; make acceptance step-scoped and
consulted by the probe (or record usage via a real back-fill verb). E-8,
E-9, E-11 ride the same engine batch. E-5/E-6 (offer economics) may follow;
H-11's staged waves make them tolerable meanwhile.

## Salvage

The three implemented changes stay UNCOMMITTED — each carries open findings.
The syntheses (ARTIFACT-23/33/35) become RUN-4 issue-body inputs: repair
issues authored with findings baked in (P-2's lesson), narrow scopes (P-5),
staged waves (H-11), gates restored per DKT-74 + a real scope gate (P-1).
The tree diff is the base RUN-4 works from.

## Recorded absences (per pre-registration §8)

No old-fleet A/B was run; no model self-metrics were consulted. The D14
post-run audit: trust store ends with exactly the three operator-approved
entries (build, tests, scope); every gate resolution recognized.

## One line for the record

The predecessor fleet never once said why it was stuck. This system
documented its own wedge to the two source lines that compose it, asked
permission to stop, and left every byte reviewable. That is the property
the whole design was purchased for — observed, not asserted.
